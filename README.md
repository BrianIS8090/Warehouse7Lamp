# 📦 Склад Online v5.1

## Описание проекта

**Склад Online v5.1** — это полнофункциональная веб-система управления складом с поддержкой проектного учета, спецификаций, автоматического резервирования товаров, системы прав доступа и полного журналирования действий. Приложение обеспечивает учет товародвижения, управление проектами и формирование спецификаций с автоматическим списанием.

### Основные возможности

- 📊 **Управление товарами** — полный CRUD, категории, поиск, сортировка, фильтрация, отслеживание изменений цен
- 📦 **Учет движения** — приход/расход товаров с историей операций и возможностью отмены
- 🎯 **Управление проектами** — заказы с датами, бюджетами, статусами и документацией
- 📋 **Спецификации** — формирование комплектов товаров с автоматическим резервированием и множителем количества
- 📈 **Аналитический дашборд** — стоимость склада, критичные остатки, горящие сроки
- 👥 **Система прав доступа** — 7 ролей пользователей с настраиваемыми правами
- 📝 **Журнал действий** — полное логирование всех операций с фильтрацией и экспортом
- 🔄 **Облачная синхронизация** — Firebase + localStorage для оффлайн-работы
- 🌓 **Темная/светлая тема** — поддержка обеих цветовых схем
- 📱 **Адаптивный дизайн** — работает на десктопе, планшетах и смартфонах
- 💾 **Экспорт/Импорт** — резервное копирование, миграция данных, экспорт в Excel
- 📎 **Работа с файлами** — поддержка Google Drive, просмотр изображений и PDF
- 👤 **Профили пользователей** — персональные настройки и аватары

---

## 📁 Структура проекта

```
BD _Cursor/
├── index.html              # Главный HTML файл приложения
├── favicon.png             # Иконка сайта
├── README.md               # Документация проекта
├── ENV_SETUP.md            # Инструкция по настройке переменных окружения
├── env.js                  # Конфигурация Firebase (НЕ коммитится в Git!)
├── env.js.example          # Шаблон конфигурации для разработчиков
├── .gitignore              # Игнорируемые файлы для Git
│
├── css/
│   └── style.css           # Пользовательские стили и анимации
│
├── js/
│   ├── config.js           # Загрузка конфигурации Firebase из env.js
│   ├── app-state.js        # Глобальное состояние приложения
│   ├── app.js              # Основная логика и навигация
│   ├── auth.js             # Авторизация и аутентификация
│   ├── db.js               # Работа с базой данных и синхронизация
│   ├── warehouse.js        # Логика складского учета
│   ├── movements.js        # Управление движением товаров
│   ├── projects.js         # Управление проектами
│   ├── specs.js            # Управление спецификациями
│   ├── dashboard.js        # Аналитический дашборд
│   ├── ui.js               # UI компоненты и модальные окна
│   ├── utils.js            # Вспомогательные функции
│   ├── theme.js            # Управление темой оформления
│   ├── permissions.js      # Система прав доступа и ролей
│   └── help.js             # Система справки и документации
│
└── images/
    └── main.png            # Логотип приложения
```

---

## 🚀 Установка и запуск

### Требования

- Современный веб-браузер (Chrome, Firefox, Safari, Edge)
- Подключение к интернету (для облачной синхронизации)
- Firebase аккаунт (опционально, работает и без него в демо-режиме)

### Быстрый старт

1. **Клонируйте или скачайте проект**
   ```bash
   git clone <repository-url>
   cd "BD _Cursor"
   ```

2. **Настройте переменные окружения**
   ```bash
   # Скопируйте файл примера
   cp env.js.example env.js
   
   # Откройте env.js и замените значения на ваши из Firebase Console
   ```
   ⚠️ **ВАЖНО**: Файл `env.js` содержит конфиденциальные данные и НЕ должен коммититься в Git!
   
   📖 Подробная инструкция: см. [ENV_SETUP.md](ENV_SETUP.md)

3. **Откройте файл `index.html` в браузере**
   - Двойной клик по файлу, или
   - Используйте локальный сервер (рекомендуется):
   ```bash
   # Python 3
   python -m http.server 8000
   
   # Node.js (с установленным http-server)
   npx http-server
   ```

4. **Выберите режим входа:**
   - **Firebase вход** — для облачной синхронизации (требуется настройка)
   - **Демо вход** — локальный режим без авторизации

---

## 🔐 Настройка Firebase (опционально)

### Шаг 1: Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Создайте новый проект
3. Включите **Realtime Database**
4. Настройте **Authentication** → Email/Password

### Шаг 2: Настройка Security Rules

В Realtime Database добавьте правила безопасности:

```json
{
  "rules": {
    "warehouse_v4": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
```

### Шаг 3: Получение конфигурации

1. В настройках проекта Firebase найдите **Web App Configuration**
2. Скопируйте конфигурацию
3. Создайте файл `env.js` из шаблона `env.js.example`
4. Замените данные в файле `env.js`:

```javascript
const ENV = {
    FIREBASE_API_KEY: "YOUR_API_KEY",
    FIREBASE_AUTH_DOMAIN: "YOUR_PROJECT_ID.firebaseapp.com",
    FIREBASE_DATABASE_URL: "https://YOUR_PROJECT_ID.firebaseio.com",
    FIREBASE_PROJECT_ID: "YOUR_PROJECT_ID",
    FIREBASE_STORAGE_BUCKET: "YOUR_PROJECT_ID.appspot.com",
    FIREBASE_MESSAGING_SENDER_ID: "YOUR_MESSAGING_ID",
    FIREBASE_APP_ID: "YOUR_APP_ID",
    FIREBASE_MEASUREMENT_ID: "YOUR_MEASUREMENT_ID"
};
```

⚠️ **ВАЖНО**: 
- Файл `env.js` уже добавлен в `.gitignore` и не будет коммититься
- Делитесь только `env.js.example` с другими разработчиками
- Подробная инструкция: [ENV_SETUP.md](ENV_SETUP.md)

### Шаг 4: Создание пользователя

В Firebase Console → Authentication → Users → Add User:
- Email: `admin@sklad.ru` (или любой другой)
- Password: укажите надежный пароль

---

## 📖 Руководство пользователя

### 1. Управление товарами (Склад)

#### Добавление товара
1. Перейдите на вкладку **"Склад"**
2. Нажмите кнопку **"+ Товар"**
3. Заполните форму:
   - **Наименование** — название товара
   - **Производитель** — бренд или производитель
   - **Категория** — группа товара (создается автоматически)
   - **Начальный остаток** — количество на складе
   - **Единица измерения** — шт., п.м., уп., кг
   - **Цена** — стоимость за единицу
   - **Характеристики** — дополнительное описание
   - **Фото URL** — ссылка на изображение
   - **Файл URL** — ссылка на документ/паспорт товара

#### Редактирование товара
- Кликните на строку товара в таблице
- Откроется карточка с двумя вкладками:
  - **Инфо** — редактирование основных данных
  - **История** — операции и использование в проектах

#### Быстрые операции
В таблице склада доступны кнопки:
- 🟢 **Стрелка вниз** — быстрый приход товара
- 🔴 **Стрелка вверх** — быстрое списание товара

#### Поиск и фильтрация
- **Поиск** — введите название или производителя в строку поиска
- **Категории** — выберите категорию в правой панели
- **Сортировка** — кликните на заголовок колонки для сортировки

#### Пагинация
Выберите количество отображаемых товаров:
- **20** — показать 20 товаров на странице
- **100** — показать 100 товаров
- **Все** — показать все товары без пагинации

#### Массовое удаление
1. Включите в **Настройках** → "Массовый выбор"
2. Выберите товары чекбоксами
3. Нажмите **"Удалить выбранные"**

### 2. Движение товаров (Приход/Уход)

#### Операция прихода
1. Перейдите на вкладку **"Приход/Уход"**
2. Выберите товар из выпадающего списка
3. Введите количество
4. Укажите номер накладной (опционально)
5. Нажмите **"Приход"** (зеленая кнопка)

#### Операция расхода
Аналогично приходу, но нажмите **"Списание"** (красная кнопка)

#### История операций
- Отображаются последние 50 операций
- Можно отменить операцию кнопкой 🗑️

### 3. Управление проектами

#### Создание проекта
1. Перейдите на вкладку **"Проекты"**
2. Нажмите **"+ Проект"**
3. Заполните данные:
   - **Год** — год выполнения
   - **Номер заявки** — уникальный номер
   - **Наименование** — название проекта
   - **Описание** — подробное описание
   - **Заказчик** — имя клиента
   - **Дата начала** — старт проекта
   - **Срок сдачи** — дедлайн
   - **Бюджет** — плановая стоимость

#### Работа с проектом
- **Клик на название** — открыть карточку проекта
- В карточке две вкладки:
  - **Основная информация** — редактирование данных
  - **Спецификации** — список всех спецификаций проекта

#### Закрытие проекта
1. Нажмите **"Закрыть"** в таблице проектов
2. Система проверит наличие товаров
3. Автоматически спишутся все товары из черновых спецификаций
4. Проект перейдет в статус "Закрыт"

#### Копирование проекта
В карточке проекта нажмите **"Копия"** — создаст полную копию проекта со всеми спецификациями

#### Удаление проекта
1. Включите в **Настройках** → "Удаление проектов"
2. В карточке проекта нажмите **"Удалить проект"**

### 4. Спецификации

#### Создание спецификации
1. Перейдите на вкладку **"Спецификации"**
2. Выберите проект в левой панели
3. Нажмите **"+ Добавить спецификацию"**
4. Выберите:
   - **Создать новую** — пустая спецификация
   - **Использовать существующую** — копировать из другого проекта

#### Добавление товаров в спецификацию
1. В поле поиска начните вводить название товара
2. Выберите товар из выпадающего списка
3. Укажите количество
4. Нажмите **"+"**

#### Добавление нестандартного изделия
1. Нажмите кнопку **"+"** (желтая)
2. Заполните:
   - Наименование
   - Единица измерения
   - Количество
   - Цена

#### Множитель количества
В верхней части спецификации есть поле **"Количество"**:
- Установите значение > 1 для умножения всех позиций
- Например, если спецификация на 1 изделие, а нужно 10 — укажите 10

#### Печать спецификации
- **"Печать"** — распечатать с ценами
- **"Печать для производства"** — без цен

#### Списание спецификации
1. Нажмите **"Списать"**
2. Товары автоматически спишутся со склада
3. Спецификация перейдет в статус "Списана"

### 5. Аналитический дашборд

Показывает ключевые метрики:

- **💰 Стоимость склада** — общая стоимость всех товаров
- **✅ Активные проекты** — количество проектов в работе + их бюджет
- **⚠️ Мало товара** — товары с отрицательным свободным остатком
- **🔥 Горят сроки** — проекты со сроком сдачи в течение 7 дней

#### Виджеты
- **Срочные проекты** — проекты с ближайшими дедлайнами
- **Требуют внимания** — товары с критическими остатками
- **Последние операции** — последние 5 движений товаров

### 6. Настройки

Доступны через кнопку ⚙️ в шапке:

#### Переключатели
- **Массовый выбор** — включить чекбоксы для удаления товаров
- **Удаление проектов** — разрешить удаление проектов из карточки

#### Резервное копирование
- **Скачать базу (Backup)** — экспорт в JSON
- **Загрузить базу (JSON)** — импорт из резервной копии
- **Импорт из старого CSV** — миграция из legacy-систем

#### Экспорт в Excel
- **Экспорт в Excel (CSV)** — выгрузка товаров в CSV формат

### 7. Темы оформления

Кнопка 🌙/☀️ в шапке переключает между светлой и темной темами.

---

## 🔧 API и архитектура

### Структура данных

#### База данных (db)
```javascript
{
  items: [        // Массив товаров
    {
      id: number,
      name: string,
      manuf: string,
      cat: string,
      qty: number,
      unit: string,
      cost: number,
      img: string,
      file: string,
      chars: string
    }
  ],
  projects: [     // Массив проектов
    {
      id: number,
      year: string,
      num: string,
      name: string,
      desc: string,
      client: string,
      start: string,
      end: string,
      cost: number,
      status: 'active' | 'closed',
      file: string
    }
  ],
  specs: {        // Спецификации по проектам
    [projectId]: [
      {
        id: string,
        name: string,
        status: 'draft' | 'committed',
        date: string,
        quantity: number,
        items: [
          {
            itemId: number,
            qty: number,
            isCustom: boolean,
            name?: string,
            cost?: number,
            unit?: string
          }
        ]
      }
    ]
  },
  movements: [    // История движений
    {
      id: string,
      date: string,
      type: 'in' | 'out',
      itemId: number,
      itemName: string,
      qty: number,
      invoice?: string
    }
  ]
}
```

### Ключевые функции

#### Управление данными
- `init()` — загрузка данных из Firebase/localStorage
- `save()` — сохранение в localStorage + пометка несохраненных изменений
- `syncWithCloud()` — синхронизация с Firebase

#### Склад
- `renderWarehouse()` — отрисовка таблицы товаров
- `filterCat(category)` — фильтрация по категории
- `getReserve(itemId)` — расчет резерва товара
- `openItemCard(id)` — открытие карточки товара

#### Проекты
- `renderProjects()` — отрисовка списка проектов
- `closeProject(id)` — закрытие проекта с списанием
- `duplicateProject(id)` — копирование проекта

#### Спецификации
- `loadSpec(specId)` — загрузка спецификации для редактирования
- `addToSpec()` — добавление товара в спецификацию
- `commitSpec()` — списание спецификации
- `updateSpecQuantity(qty)` — изменение множителя

#### UI
- `showToast(message)` — показать уведомление
- `showLoader(visible)` — показать/скрыть loader
- `openModal(id)` — открыть модальное окно
- `closeModal(id)` — закрыть модальное окно

### Оптимизация производительности

#### Кеширование резервов
```javascript
let reservesCache = {};
let reservesCacheTimestamp = 0;
const RESERVES_CACHE_TTL = 1000; // 1 секунда
```

#### Debounce для поиска
```javascript
const debouncedRenderWarehouse = debounce(() => {
    renderWarehouse();
}, 300);
```

#### DocumentFragment для рендера
```javascript
const fragment = document.createDocumentFragment();
// ... добавление элементов
tbody.appendChild(fragment); // Одна операция DOM
```

---

## 🎨 Кастомизация

### Изменение цветовой схемы

Отредактируйте Tailwind конфигурацию в `index.html`:

```javascript
tailwind.config = {
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                // Ваши цвета
            }
        }
    }
}
```

### Добавление новой категории товаров

Категории создаются автоматически при добавлении товара. Для предустановленных категорий измените функцию `seedData()` в `db.js`.

### Изменение единиц измерения

В файлах `warehouse.js`, `projects.js`, `specs.js` найдите селекторы с единицами:

```html
<select id="cardUnit">
    <option value="шт.">шт.</option>
    <option value="п.м.">п.м.</option>
    <option value="уп.">уп.</option>
    <option value="кг">кг</option>
    <!-- Добавьте свои -->
</select>
```

---

## 🐛 Известные ограничения

1. **Одновременная работа** — не поддерживается real-time коллаборация (последний синхронизировавшийся перезапишет данные)
2. **История изменений** — нет version control для данных
3. **Прикрепление файлов** — только через URL (нет загрузки на сервер)
4. **Печать** — базовая функциональность (без штрих-кодов/QR)
5. **Мобильный** — адаптивный, но некоторые функции удобнее на десктопе

---

## ❓ FAQ

### Как восстановить данные после потери?

1. Регулярно делайте **Backup** через Настройки
2. При использовании Firebase данные сохраняются в облаке автоматически

### Можно ли использовать без интернета?

Да, в демо-режиме или если данные уже загружены из Firebase — они сохраняются в localStorage.

### Как добавить нового пользователя?

В Firebase Console → Authentication → Add User. После создания пользователь может войти с указанными credentials.

### Почему не работает синхронизация?

1. Проверьте подключение к интернету
2. Убедитесь, что Firebase настроен корректно
3. Проверьте Security Rules в Firebase Console

### Как изменить логотип?

Замените файл `images/main.png` на свой (рекомендуемый размер: 64x64px)

---

## 🔒 Безопасность

### Рекомендации

1. **Защита конфигурации**
   - ⚠️ **НЕ коммитьте** файл `env.js` в Git (уже добавлен в `.gitignore`)
   - Используйте `env.js.example` как шаблон для других разработчиков
   - Храните реальные ключи в безопасном месте (менеджер паролей, секреты CI/CD)
   - При деплое создавайте `env.js` на сервере вручную или через переменные окружения

2. **Firebase Security Rules** — ограничьте доступ к базе данных:
   ```json
   {
     "rules": {
       "warehouse_v4": {
         ".read": "auth != null",
         ".write": "auth != null"
       }
     }
   }
   ```

3. **Сильные пароли** — используйте сложные пароли для пользователей

4. **HTTPS** — размещайте на хостинге с SSL сертификатом

5. **Резервные копии** — делайте регулярные backup

---

## 📊 Производительность

### Оптимизация при большом количестве товаров

- Используйте **пагинацию** (20-100 товаров на странице)
- Включите **кеширование резервов** (уже реализовано)
- **Дебаунс поиска** предотвращает лишние рендеры

### Рекомендуемые лимиты

- Товары: до 10,000 позиций (с пагинацией)
- Проекты: до 1,000 активных проектов
- Движения: до 50,000 операций (старые можно архивировать)

---

## 🤝 Поддержка и развитие

### Возможности для развития

- [ ] TypeScript для типобезопасности и лучшей поддержки IDE
- [ ] Unit/Integration тесты (Jest, Cypress)
- [ ] PWA для оффлайн работы с кешированием ресурсов
- [ ] Расширенная аналитика и графики (Chart.js, D3.js)
- [ ] Штрих-коды и QR-коды для быстрого учета
- [ ] API для интеграции с внешними системами
- [ ] Экспорт в Excel с форматированием и диаграммами
- [ ] Email/Telegram уведомления о важных событиях
- [ ] Мультиязычность (i18n)
- [ ] Версионирование данных и история изменений
- [ ] Мобильное приложение (React Native/Flutter)
- [ ] Интеграция с 1С и другими ERP системами
- [ ] Автоматические отчеты по расписанию
- [ ] Визуальный конструктор спецификаций
- [ ] Система уведомлений о критических остатках
- [ ] Интеграция с системами учета рабочего времени
- [ ] Автоматическое создание заказов поставщикам
- [ ] Система резервирования товаров по заказам клиентов
- [ ] Интеграция с системами электронного документооборота

---

## 📝 Версии

### v5.1 (текущая)
- ✨ **Система прав доступа** — 7 ролей пользователей с настраиваемыми правами
- ✨ **Журнал действий** — полное логирование всех операций с фильтрацией и экспортом
- ✨ **Улучшенная аутентификация** — профили пользователей, аватары, персональные настройки
- ✨ **Работа с файлами** — поддержка Google Drive, просмотр изображений и PDF документов
- ✨ **Отслеживание цен** — автоматическое логирование изменений стоимости товаров
- ✨ **Улучшенный UI** — анимации модальных окон, индикаторы загрузки, улучшенная навигация
- ✨ **Полный рефакторинг кода** — модульная архитектура с разделением ответственности
- ✨ **Темная тема** — поддержка светлой и темной цветовых схем
- ✨ **Пагинация товаров** — оптимизация производительности при больших объемах
- ✨ **Массовые операции** — групповое удаление товаров, настраиваемые права
- ✨ **Аналитический дашборд** — расширенная аналитика с критичными показателями
- ✨ **Быстрые операции** — мгновенный приход/расход товаров прямо из таблицы
- ✨ **Множитель количества** — масштабирование спецификаций для серийного производства
- ✨ **Копирование проектов** — дублирование проектов со всеми спецификациями
- 🐛 **Исправления** — проблемы с резервированием, производительностью и совместимостью

---

## 📄 Лицензия

Проект создан для внутреннего использования. Все права защищены.

---

## 👨‍💻 Автор

Разработано с ❤️ для эффективного управления складом.

**Последнее обновление документации:** 26 ноября 2025

---

## 🆘 Техническая поддержка

При возникновении вопросов или проблем:

1. Проверьте этот документ (README.md)
2. Откройте консоль браузера (F12) для просмотра ошибок
3. Сделайте резервную копию данных перед экспериментами
4. Протестируйте в демо-режиме перед внесением изменений

---

**Приятной работы! 📦✨**



