# 🤝 Руководство по контрибуции — Склад Online v5.0

Спасибо за интерес к улучшению проекта! Мы ценим любой вклад.

---

## 📋 Содержание

1. [Как помочь проекту](#как-помочь-проекту)
2. [Процесс контрибуции](#процесс-контрибуции)
3. [Стандарты кода](#стандарты-кода)
4. [Commit-сообщения](#commit-сообщения)
5. [Тестирование](#тестирование)
6. [Документация](#документация)
7. [Сообщения об ошибках](#сообщения-об-ошибках)
8. [Предложения функций](#предложения-функций)

---

## 🎯 Как помочь проекту

### Виды контрибуции

- 🐛 **Исправление багов** — находите и исправляйте ошибки
- ✨ **Новые функции** — добавляйте новую функциональность
- 📚 **Документация** — улучшайте и дополняйте документы
- 🎨 **Дизайн** — улучшайте UI/UX
- 🌍 **Переводы** — добавляйте поддержку других языков
- ⚡ **Оптимизация** — улучшайте производительность
- 🧪 **Тестирование** — пишите тесты
- 💡 **Идеи** — предлагайте улучшения

---

## 🔄 Процесс контрибуции

### 1. Подготовка

```bash
# Форкните репозиторий на GitHub

# Клонируйте свой форк
git clone https://github.com/YOUR_USERNAME/warehouse.git
cd warehouse

# Добавьте upstream remote
git remote add upstream https://github.com/ORIGINAL_OWNER/warehouse.git
```

### 2. Создание ветки

```bash
# Обновите main ветку
git checkout main
git pull upstream main

# Создайте новую ветку для вашей задачи
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

**Соглашение по именованию веток:**
- `feature/` — новая функциональность
- `fix/` — исправление бага
- `docs/` — изменения в документации
- `refactor/` — рефакторинг кода
- `test/` — добавление тестов
- `style/` — изменения стилей/форматирования

### 3. Разработка

1. **Внесите изменения** в код
2. **Тестируйте** локально
3. **Проверьте код** на ошибки
4. **Обновите документацию** при необходимости

### 4. Коммит изменений

```bash
# Добавьте файлы
git add .

# Сделайте коммит (см. формат ниже)
git commit -m "feat: добавлена поддержка штрих-кодов"
```

### 5. Push и Pull Request

```bash
# Отправьте изменения в свой форк
git push origin feature/your-feature-name
```

Затем на GitHub:
1. Откройте Pull Request
2. Заполните шаблон описания
3. Ждите ревью

### 6. Код-ревью

- Отвечайте на комментарии рецензентов
- Вносите запрошенные изменения
- После одобрения PR будет смержен

---

## 📝 Стандарты кода

### JavaScript

#### Форматирование

```javascript
// ✅ Правильно
function calculateTotal(items) {
    let sum = 0;
    items.forEach(item => {
        sum += item.price * item.qty;
    });
    return sum;
}

// ❌ Неправильно
function calculateTotal(items){
  let sum=0
  items.forEach(item=>{sum+=item.price*item.qty})
  return sum
}
```

#### Правила

- **Отступы:** 4 пробела (не табы)
- **Кавычки:** одинарные `'` для строк
- **Точка с запятой:** обязательна
- **Скобки:** всегда, даже для однострочных блоков
- **Именование:**
  - `camelCase` для переменных и функций
  - `PascalCase` для классов (если будут)
  - `UPPER_CASE` для констант
  - Понятные имена, не сокращения

```javascript
// ✅ Правильно
const MAX_ITEMS_PER_PAGE = 100;
let currentPage = 1;
function renderWarehouse() { ... }

// ❌ Неправильно
const max = 100;
let cp = 1;
function rw() { ... }
```

#### Комментарии

```javascript
// ✅ Хорошие комментарии
// Расчёт резерва товара с кешированием для производительности
function getReserve(itemId) { ... }

// ❌ Плохие комментарии
// Функция
function getReserve(itemId) { ... }
```

### HTML

```html
<!-- ✅ Правильно -->
<div class="flex items-center gap-2">
    <button onclick="saveData()" class="btn-primary">
        Сохранить
    </button>
</div>

<!-- ❌ Неправильно -->
<div class="flex items-center gap-2"><button onclick="saveData()" class="btn-primary">Сохранить</button></div>
```

### CSS

```css
/* ✅ Правильно */
.button-primary {
    background-color: #2563eb;
    padding: 0.5rem 1rem;
    border-radius: 0.375rem;
}

/* ❌ Неправильно */
.button-primary{background-color:#2563eb;padding:0.5rem 1rem;border-radius:0.375rem;}
```

---

## 💬 Commit-сообщения

Следуйте [Conventional Commits](https://www.conventionalcommits.org/):

### Формат

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы

- `feat` — новая функциональность
- `fix` — исправление бага
- `docs` — изменения в документации
- `style` — форматирование, отступы (не влияет на функциональность)
- `refactor` — рефакторинг кода
- `perf` — улучшение производительности
- `test` — добавление тестов
- `chore` — обслуживание, зависимости

### Примеры

```bash
# Новая функция
feat(warehouse): добавлена пагинация товаров

# Исправление бага
fix(specs): исправлен расчёт резервов с множителем

# Документация
docs(readme): обновлена инструкция по установке

# Производительность
perf(warehouse): оптимизирован рендер через DocumentFragment

# Рефакторинг
refactor(db): вынесен код синхронизации в отдельную функцию
```

---

## 🧪 Тестирование

### Перед коммитом

1. **Ручное тестирование:**
   - Протестируйте изменённую функциональность
   - Проверьте, что не сломалась другая функциональность
   - Проверьте на разных устройствах (десктоп, планшет, мобильный)
   - Проверьте в разных браузерах (Chrome, Firefox, Safari)

2. **Проверки:**
   - Открыйте консоль браузера (F12) — нет ошибок?
   - Проверьте темную и светлую темы
   - Проверьте демо-режим и Firebase-режим

### Unit-тесты (будущее)

```javascript
// Пример теста (когда будет Jest)
describe('getReserve', () => {
    test('возвращает 0 для товара без резервов', () => {
        expect(getReserve(999)).toBe(0);
    });
    
    test('корректно рассчитывает резерв', () => {
        db.specs = {
            1: [{
                status: 'draft',
                quantity: 2,
                items: [{ itemId: 1, qty: 5 }]
            }]
        };
        expect(getReserve(1)).toBe(10); // 5 * 2
    });
});
```

---

## 📚 Документация

### Когда обновлять документацию

- ✅ Добавили новую функцию
- ✅ Изменили существующее поведение
- ✅ Изменили API функций
- ✅ Добавили новые файлы/модули
- ✅ Изменили структуру данных

### Какие файлы обновлять

- **README.md** — пользовательская документация
- **TECHNICAL.md** — техническая документация
- **CHANGELOG.md** — история изменений
- **Комментарии в коде** — JSDoc для функций

### Формат комментариев функций

```javascript
/**
 * Расчёт резерва товара с кешированием
 * 
 * @param {number} itemId - ID товара
 * @returns {number} Количество зарезервированного товара
 * 
 * @example
 * const reserve = getReserve(42);
 * console.log(reserve); // 15.5
 */
function getReserve(itemId) {
    // ...
}
```

---

## 🐛 Сообщения об ошибках

### Как сообщить о баге

1. **Проверьте**, что баг ещё не был сообщён в Issues
2. **Откройте новый Issue** с меткой `bug`
3. **Используйте шаблон:**

```markdown
## Описание бага
Краткое описание проблемы

## Шаги для воспроизведения
1. Открыть вкладку "Склад"
2. Нажать на товар
3. Изменить цену
4. Сохранить

## Ожидаемое поведение
Цена должна обновиться

## Фактическое поведение
Цена не меняется, старое значение остаётся

## Скриншоты
(если применимо)

## Окружение
- **Браузер:** Chrome 120
- **ОС:** Windows 11
- **Версия приложения:** 5.0.0
- **Режим:** Firebase / Demo

## Дополнительная информация
- Ошибки в консоли (F12)
- Шаги, которые уже пробовали
```

---

## 💡 Предложения функций

### Как предложить новую функцию

1. **Откройте Issue** с меткой `enhancement`
2. **Опишите проблему**, которую решает функция
3. **Предложите решение**
4. **Приведите примеры** использования

```markdown
## Проблема
Сложно отследить изменения цен на товары

## Предлагаемое решение
Добавить историю изменений цен с датами и пользователями

## Альтернативы
- Логирование всех изменений
- Версионирование записей

## Дополнительный контекст
Скриншоты из других систем, примеры
```

---

## ✅ Чек-лист перед Pull Request

- [ ] Код следует стандартам проекта
- [ ] Добавлены комментарии для сложной логики
- [ ] Обновлена документация (если нужно)
- [ ] Протестировано вручную
- [ ] Нет ошибок в консоли
- [ ] Работает в Chrome, Firefox, Safari
- [ ] Работает на мобильных устройствах
- [ ] Проверена тёмная и светлая темы
- [ ] Commit-сообщения следуют формату
- [ ] Обновлён CHANGELOG.md

---

## 📞 Контакты

### Нужна помощь?

- 💬 Откройте Discussion на GitHub
- 📧 Напишите на email (указать ваш email)
- 💡 Задайте вопрос в Issue

---

## 🎉 Спасибо!

Ваш вклад делает проект лучше. Каждый Pull Request, Issue, комментарий важны.

**Счастливого кодинга! 🚀**

---

## 📜 Code of Conduct

### Наши стандарты

Мы стремимся создать открытую и дружелюбную среду:

✅ **Приветствуется:**
- Уважительное общение
- Конструктивная критика
- Помощь новичкам
- Открытость к другим мнениям

❌ **Недопустимо:**
- Оскорбления и нападки
- Троллинг и провокации
- Спам и реклама
- Нарушение конфиденциальности

### Ответственность

Maintainers проекта имеют право удалять комментарии, коммиты, issues и PR, которые нарушают эти правила.

---

**Последнее обновление:** 24 ноября 2025


